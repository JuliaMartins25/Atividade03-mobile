import { View, Text} from "react-native";

export default function Home(){
    return (
        <View>
            <Text styles = {{flex: 1, justifyContent: "center", alignItems: "center"}} >Home</Text>
        </View>
    );
}